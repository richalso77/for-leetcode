class Solution {
public:
    bool hasTrailingZeros(vector<int>& nums) {
        int check = 0;
        for (const int &temp: nums){
            if(temp % 2)    continue;
            check++;
            if (check == 2) return true;
        }
        return false;
    }
};
